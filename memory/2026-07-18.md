# Session — 18 July 2026

Implemented remaining items from Nahuel's 27-Jun spec.

## 1.2 Logo timing
`IntroHome.tsx`: reveal moved 4000ms → 3000ms (1s earlier). Exit unchanged at 7500ms.

## 1.3 Typography
- `globals.css`: `html { font-size: calc(100% + 1pt) }` — global +1pt (bump to 2pt if Nahuel wants more).
- `body { text-transform: uppercase }` — all text uppercase.
- Removed all ` lowercase` utility classes (7 files) so nothing overrides the global.
- Futura NOT adopted: paid license (Bauer/Neufville). Jost stays. Flagged to Nahuel.

## 1.4 Portrait logo position toggle
- `IntroHome.tsx`: `PORTRAIT_LOGO_POSITION: 'top' | 'bottom'` const (currently `'top'`).
- `globals.css`: `.intro-logo-bottom` overrides `--intro-logo-y` to `calc(50vh - 2.75rem)` under `max-width: 639px`.
- Flip the const to switch. Pending Nahuel's choice.

## 2.1 Clouds crop (portrait)
- `globals.css`: `.hero-portrait-top` → `background-position: center top !important` under `@media (orientation: portrait)`.
- `PageHero` + `Footer`: new `portraitTop?: boolean` prop adds the class.
- Applied on `director_de_fotografia/page.tsx` and `.../proyectos/page.tsx` (hero + footer).

## 4.3 Director bio
Photo now RIGHT, text LEFT on sm+ (photo `order-2`, text `order-1`) — same pattern as fotografo. Mobile: text first.

## Open items
- 4.2 reel layout: spec says full-bleed, 28-Jun session set 90vw deliberately — A/B pending (Jamie: "which looks best").
- 4.4 `FOTO FONDO MENÚ.jpg` upload pending Nahuel's crop (dimensions in flag report).
- Photography grid still placeholders — Nahuel to supply 3 photos per collection.
- WP upload path: Hostinger hPanel → WP Admin (Jamie logs in via Chrome extension).

## Environment note
Cowork sandbox mount can serve stale/truncated views of files edited on the host side — if tsc reports absurd syntax errors in files you just edited, rewrite the file from the sandbox side (cat heredoc) or re-check on host before debugging. `tsc --noEmit` clean; scoped eslint clean; full `npm test` (Playwright) not run — sandbox can't run Windows node_modules; run on host.
