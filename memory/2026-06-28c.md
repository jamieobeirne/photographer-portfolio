# Session — 28 June 2026 (evening)

## Director page spacing system

All three gaps (header→video, video→bio, bio→footer) use the same
responsive Tailwind scale:
- Mobile: `mt-8` / `mb-8` (32px / 2rem)
- Landscape: `sm:mt-16` / `sm:mb-16` (64px / 4rem)

Spacing uses `rem` (Tailwind default) — correct for layout gaps.
`em` is for component-internal padding that should scale with font size.

## Director video width

Set to `w-[90vw] mx-auto block` — 90% of viewport, centered.

## Fotografo grid width + spacing

- Grid container: `w-[90vw] mx-auto` — 90% of viewport, centered
- Top gap: `mt-8 sm:mt-16` (matches director page rhythm)
- Bottom gap: `mb-8 sm:mb-16` (symmetric with top)

Note: grid cell sizes now reference `(90vw - 6px) / 3` implicitly
since the grid is no longer full-width. The bio photo size
(`w-[90vw] sm:w-[calc((100vw-6px)/3)]`) was set before this change
and may need revisiting if pixel-perfect grid-cell match is required.

## Unified bio sections — all three main pages

fotografo, director, and director_de_fotografia now share identical
bio teaser design:

| Property | Value |
|---|---|
| Layout | `flex flex-col sm:flex-row` |
| Photo | `order-2 sm:order-1 w-full sm:w-[45%] aspect-[4/3] sm:aspect-auto object-cover` |
| Text | `order-1 sm:order-2 flex-1 flex-col justify-center px-8 py-12 sm:px-14 sm:py-16 lg:px-20 lg:py-20` |
| Label | `ACERCA DE` — `text-white/30 text-[0.52rem] tracking-[0.4em] mb-6` |
| Body | `text-white/65 clamp(0.9rem,1.6vw,1.05rem) font-light leading-relaxed lowercase mb-8` |
| Link | `BIOGRAFÍA` — `text-white/40 border border-white/15 px-7 py-3 self-start` |
| Bottom gap | `mb-8 sm:mb-16` |
| Mobile | Text first (order-1), photo below (order-2) |
| Landscape | Photo left, text right |

### Per-page photo sources
- fotografo → `fotopage_bio` ACF field (`foto.bio_.png`, 1421×1067)
- director → `director_bio_foto` ACF field (`director_bio.jpg`)
- director_de_fotografia → `cinematographer_nahuel` ACF field (mirrored: `scaleX(-1)`)

### Per-page biography links
- fotografo → `/fotografo/biografia`
- director → `/director/biografia`
- director_de_fotografia → `/director_de_fotografia/biografia`

## director_de_fotografia page cleanup

Removed the previous elaborate bio section (NAHUEL BEADE heading,
CINEMATÓGRAFO subtitle, 3-paragraph lorem ipsum, DESCARGAR CV button,
orange accent, divider line) in favour of the unified simple design.
