# Session — 28 June 2026 (night)

## Bio sections — 90vw constraint applied to all pages

All four bio teaser sections now share identical sizing:
`w-[90vw] mx-auto` on the `<section>` element itself (border-t is also constrained to 90vw).

Pages affected: fotografo, director, director_de_fotografia, contacto.

## Contact page bio section added

- Photo: `cinematographer_nahuel` ACF field (fetched alongside `fondo_contacto` and `main_logo_new`)
- Layout: identical to other pages — photo left / text right on landscape, text first / photo below on mobile
- Link: BIOGRAFÍA → `/director_de_fotografia/biografia`
- Width: `w-[90vw] mx-auto`

## Fotografo bio photo — right side on landscape

Photo is on the RIGHT in landscape (text left, photo right). Uses natural DOM order:
- Photo div: `order-2` only (no `sm:order-1` override)
- Text div: `order-1` only (no `sm:order-2` override)
- Mobile: text first (order-1), photo below (order-2) — same as other pages

## Director de Fotografía — reels content 90vw

Reels section inner content wrapped in `w-[90vw] mx-auto` div.
REELS label padding changed from `px-6 sm:px-12` to no padding (centering handled by wrapper).

## Lint & test status (as of commit f2a5c0f)

- ESLint: 0 errors, 0 warnings
- TypeScript: 0 errors
- Playwright 10/10 passing:
  - `video-quality.spec.ts` — landscape ≥ 1280×720 ✓, portrait ≥ 720×1280 ✓
  - `fotografo-instagram-grid.spec.ts` — 8/8 ✓ (desktop + mobile)

### Fotografo grid spec update
"edge-to-edge" test renamed to "grid is 90vw wide and centered".
Now checks `width ≈ vw * 0.9` and `left ≈ vw * 0.05` using `toBeCloseTo(-1)`.
Removed unused `findPortfolioGrid` helper and `(window as any)` cast.
